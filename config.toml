baseurl = "https://www.feuerwehr-tittmoning.de/"
languageCode = "de-de"
title = "Freiwillige Feuerwehr Stadt Tittmoning"
paginate = "10" # Number of posts per page

disqusShortname = "" # Enable comments by entering your Disqus shortname
googleAnalytics = "" # Enable Google Analytics by entering your tracking id

disableKinds = ["home", "section", "taxonomy", "taxonomyTerm", "RSS"]
enableRobotsTXT = true

[Params]
    subtitle = "Homepage der FFW Tittmoning" # Subtitle of your site. Used in site header
    description = "Offizieller Internetauftritt der freiwilligen Feuerwehr Tittmoning - Hier erfahren Sie allerhand Wissenswertes rund um die Feuerwehr Tittmoning." # Description of your site. Used in meta description
    opengraph = false # Enable OpenGraph if true
    readmore = false # Show "Read more" button in list if true
    leftsidebar = false # Move sidebar to the left side if true
    authorbox = false # Show authorbox at bottom of pages if true
    post_navigation = false # Show post navigation at bottom of pages if true
    postSections = ["post"] # the section pages to show on home page and the "Recent articles" widget
    #postSections = ["blog", "news"] # alternative that shows more than one section's pages
    #dateformat = "2006-01-02" # change the format of dates

[Params.widgets]
    search = true # Enable "Search" widget
    recent_articles = false # Enable "Recent articles" widget
    recent_articles_num = 5 # Set the number of articles in the "Recent articles" widget
    categories = false # Enable "Categories" widget
    tags = false # Enable "Tags" widget
    tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)

    # Enable "Social" widget, if any of "social_*" set a value
    social_facebook = "Feuerwehr-Tittmoning-125470620886379"
    social_email = "mail@feuerwehr-tittmoning.de"
    